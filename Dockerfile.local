FROM nginx:alpine

# Copy the built app from dist folder to nginx html directory
COPY dist/yap-landing/ /usr/share/nginx/html/

# Copy custom nginx configuration for local testing
COPY nginx-local.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
